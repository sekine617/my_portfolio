<div class="center-block">
 <div class="card">
  <div class="card-header">
    <h3>注文内容確認</h3>
  </div>
    <div class="card-body m-4">

        <div class="form-group row">
           <label class="col-sm-3 col-form-label"><h5>受け取り希望日</h5></label>
           <div class="col-sm-9">
              <h5><%= @order.receipt_date.to_s(:date_jp) %></h5>
           </div>
       </div>

      注文商品
      <%= render partial: 'bill', locals: {cart_items: @cart_items } %>
    </div>

    <div class="card-body m-4 text-center">
      <%= render partial: 'confirmForm', locals: {cart_items: @cart_items, order: @order} %>
    </div>
 </div>
</div>